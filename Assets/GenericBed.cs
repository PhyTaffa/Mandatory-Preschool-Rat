using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GenericBed : MonoBehaviour
{
    [SerializeField] public bool hasPatient = true;
    [SerializeField] public GameObject bedBindedTo = null;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
